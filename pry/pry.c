#include <cs50.h>

#include <stdio.h>

int main(void)
{
    int totalrow;
    

    while
    {    // for total row
        totalrow = get_int("Size: ");
        scanf("%d",&totalrow)
    }
           // for row
    for row= 1 row <= totalrow; row++)

    {       //for space
        for ( space = 1; space <= (totalrow-row); space++)

            printf(" ");
        }
             // for symbol 

    for ( symbol = 1; symbol <= (2*row)-1; symbol++)
        {  
            printf("#"); 
            }

    { 
        printf("\n");
    }
}
